/**
 * DS studio — Go To Top Render :: Inject (Stub)
 * 已合併至 go-top.render.combined.js。
 * 本檔僅保留供測試載入器相容，載入時引入合併檔案。
 */
if (typeof require !== 'undefined') {
    require('./go-top.render.combined.js');
}
