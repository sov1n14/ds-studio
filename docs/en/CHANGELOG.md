# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## Version Summary

### v4.x — Modular Architecture Refactoring

| Version | Summary |
|-|-|
| [4.32.5](changelog/v4.md#4325---2026-08-29) | Unified vertical spacing on all six popup section titles; `.card-title-actions` changed to `height: 0` with `align-items: center` so action buttons no longer stretch `.card-title-row` |
| [4.32.4](changelog/v4.md#4324---2026-08-28) | Fixed `popup.html` and `editor.html` missing `url-constants.js` script tag; `editor-window-autoclose.js` wrapped in try/catch for context invalidation; `temporary-chat-heartbeat.js` stops interval on context invalidation |
| [4.32.2](changelog/v4.md#4322---2026-08-28) | Popup layout adjustments: `showSystemTimeToggle` moved to first position in Features card; Features toggles reordered; Backup & Restore card moved to right column; disabling auto-expand messages now collapses all expanded messages; i18n cleanup (removed Chinese labels with English parentheses, added `autoExpandMessagesLabel`, fixed clear button text) |
| [4.32.0](changelog/v4.md#4320---2026-08-28) | Added "Features" card (hide thinking process, prevent auto-scroll, web search moved from UI Adjustments card); added "Auto-expand messages" feature (MutationObserver auto-clicks collapsed expand buttons, storage key `dsAutoExpandMessages`); original "Features & Export" card renamed to "Export" |
| [4.31.27](changelog/v4.md#43127---2026-08-27) | Added is/has prefix to the last four boolean identifiers: `_storedRecordsApplied`→`_hasStoredRecordsApplied`, `_locked`→`_isLocked`, `completionDetected`→`isCompletionDetected`, `showSystemTime`→`isShowSystemTime`, covering 13 production files and 24 test files totaling 98 occurrences |
| [4.31.26](changelog/v4.md#43126---2026-08-27) | Split two oversized files (go-top and sidebar-auto-hide): go-top added `go-top.observers.js`, `go-top.lifecycle.js` (425→73 lines); sidebar-auto-hide added `sidebar-auto-hide.styles.js`, `sidebar-auto-hide.observers.js` (416→211 lines) |
| [4.31.25](changelog/v4.md#43125---2026-08-27) | Removed `__getState`/`__setState`/`__resetState` test mirrors from four modules, retaining direct state access |
| [4.31.24](changelog/v4.md#43124---2026-08-27) | Test mirror migration phase 1: 86 `__getState`/`__setState`/`__resetState` calls across 11 specs converted to direct property read/write; production side exposes state objects for test access |
| [4.31.23](changelog/v4.md#43123---2026-08-27) | Split three oversized files (chatmap, export, edit-message-cleanup): chatmap 441→331, export 433→218, edit 407→221, each with new part files |
| [4.31.22](changelog/v4.md#43122---2026-08-27) | Split `storage-manager.js` into keys and rw part files (461→113 lines); constant declarations and low-level read/write operations separated |
| [4.31.21](changelog/v4.md#43121---2026-08-27) | Extracted `DEEPSEEK_TAB_URL` to shared constant file `utils/url-constants.js`, eliminating duplicate definitions in settings-routes and tab-control |
| [4.31.20](changelog/v4.md#43120---2026-08-27) | Removed `DSS_CHAT_CREATE_ENDPOINT` constant with no production consumers |
| [4.31.19](changelog/v4.md#43119---2026-08-27) | Eliminated dual session ID source in censor-reply-restore: `_resolveMessageIdFromStorage` now reads `_currentSessionId` instead of parsing URLs |
| [4.31.18](changelog/v4.md#43118---2026-08-27) | Removed 85 unreachable `globalThis` compatibility guards across 61 files; `typeof globalThis !== 'undefined' ? globalThis : window` consolidated to bare `globalThis` |
| [4.31.17](changelog/v4.md#43117---2026-08-27) | Removed test-only export aliases: `temporary-chat-toggle.js`'s `__setCacheForCrossTabSync` and `__setMasterEnabled` removed; specs now drive through real entry points |
| [4.31.16](changelog/v4.md#43116---2026-08-27) | Deleted `utils/messaging.js` and merged `broadcastActivePreset` into `tab-control`: the entire file re-implemented the query-then-send pipeline already in tab-control; net deletion of 137 spec lines and two script tags |
| [4.31.15](changelog/v4.md#43115---2026-08-27) | `censor-reply-restore.markdown` removed in-file duplicate HTML escaping implementation; `_renderInline` now calls same-file `_escapeHtml` |
| [4.31.14](changelog/v4.md#43114---2026-08-27) | Editor window URL parsing now provided by `utils/window-control`'s `getExtensionUrl()`, eliminating popup-layer direct calls to `chrome.runtime.getURL` |
| [4.31.13](changelog/v4.md#43113---2026-08-27) | `popup.live-sync` now subscribes to setting changes via `StorageManager.subscribeToSettingChanges()`, eliminating popup-layer direct calls to `chrome.storage.onChanged` |
| [4.31.12](changelog/v4.md#43112---2026-08-27) | `pending-store` moved from content layer to `background/pending-store.js` since the module's 10 `chrome.storage` calls belong outside the content layer; also removed 5 unreachable constant fallbacks |
| [4.31.11](changelog/v4.md#43111---2026-08-27) | `DSS_SETTINGS_MSG` reading unified to single accessor `getSettingsMessageTypes()`; eliminated six occurrences of three mutually exclusive patterns; also extracted `clampPercent` to eliminate duplicate clamping logic in chat-width and input-width |
| [4.31.10](changelog/v4.md#43110---2026-08-27) | Three message types now use named constants `DSS_AUTH_CAPTURED_TYPE`, `DSS_HISTORY_NAV_TYPE`, `DSS_FRAGMENT_COMPLETE_TYPE`; MAIN world production side annotated with pinned values |
| [4.31.9](changelog/v4.md#4319---2026-08-27) | Removed the last two `_getConst` copies and completed test-side constant loading; `_getConst` now has zero occurrences across the entire repo |
| [4.31.8](changelog/v4.md#4318---2026-08-27) | Removed six `_getConst` copies and their unreachable hardcoded fallbacks; all consolidated to bare `globalThis` constant reads, net removal of 48 lines |
| [4.31.7](changelog/v4.md#4317---2026-08-27) | Alarm names extracted to `background/service-worker-constants.js`; eleven specs now read production constants instead of hand-copied values |
| [4.31.6](changelog/v4.md#4316---2026-08-27) | All DeepSeek selectors moved to `ds-selectors.js`: added `INPUT_TEXTAREA_SELECTOR`, `ROLE_BUTTON_DIV_SELECTOR`, `FLOATING_BUTTON_BAR_DIV_SELECTOR`; seven consumer sites now read constants; removed zero-consumer `SEND_BUTTON_ICON_PATH_PREFIX` export |
| [4.31.5](changelog/v4.md#4315---2026-08-27) | `popup.markdown-export` added load-order fail-fast guards; `DSSTabControl` and `dsI18n` now throw explicit messages when missing |
| [4.31.4](changelog/v4.md#4314---2026-08-27) | Temporary chat enable flag write rejection now rolls back cache using generation counter, preventing never-persisted values from lingering in `_isEnabledCache` |
| [4.31.3](changelog/v4.md#4313---2026-08-27) | `chat-session-id` moved up to `utils/` and eliminated popup's duplicate regex; popup layer no longer violates layer rules by depending on content modules |
| [4.31.2](changelog/v4.md#4312---2026-08-27) | Removed `prompt-injector`'s duplicate mobile device detection; now uses IIFE scope to resolve shared `DSSMobileDevice` module |
| [4.31.1](changelog/v4.md#4311---2026-08-25) | Cross-device pending-delete queue converted to lease + heartbeat model: queue items expanded to `{ chatUuid, attemptCount, lastActiveAt }`, new constants `LEASE_TTL_MS = 600000`, `HEARTBEAT_INTERVAL_MS = 60000`; access layer added `refreshLease`/`releaseLease`/pure function `isLeaseExpired`; queue read-modify-write serialized through promise-chain mutual exclusion gate. `remediatePendingDeletes()` now takes no parameters and only deletes lease-expired items; `onStartup`, `dss-delete-retry` schedule, and sync `onChanged` all use the same gate, protecting in-use conversations on all devices; `onStartup` releases leases for locally open items before scanning so conversations open at shutdown are deleted immediately on restart; coordinator sends `DSS_RELEASE_LEASE` on complete immediate-delete failure. Added `content/temporary-chat-heartbeat.js` (tab lifecycle-bound lease renewal heartbeat) and `content/temporary-chat-sidebar-hide.js` (pending-delete conversations hidden in sidebar on all devices, using date group granularity, applying `ds-temp-chat-hidden` without removing nodes), four new message types and two sidebar selectors. Added 5 specs and `test/helpers/sidebar-fixtures.js` |
| [4.29.2](changelog/v4.md#4292---2026-08-22) | Retired two independently schedulable refactoring debts: `performDeleteFetch` expanded to superset signature (`keepalive` option + guard) so `deleteChatSession` becomes a one-line delegation, merging duplicate delete fetches; endpoint URL made private to `utils/deepseek-api.js` (`DSS_DELETE_ENDPOINT_URL` removed from constants file); `temporary-chat-constants.js` moved from `content/` to `utils/`; manifest, service worker entry points, fail-fast messages, and guidelines skill example paths updated accordingly. Test side: `temporary-chat-delete-api.spec.js` rewritten (28 tests, including delegation contract group); three T6 residual slow tests converted to virtual time (write-queue 9805ms → 22ms, total saving ~10.5 seconds). Certified 10 specs 205/205 all green |
| [4.29.1](changelog/v4.md#4291---2026-08-22) | Theme B wave 2: single-site DeepSeek obfuscated selectors consolidated to `content/ds-selectors.js` (added 30 named entries, 58 export keys); 12 consumer files switched to constants with identical string values: `censor-reply-restore` (3 files), `content-script.export.js`, `edit-message-cleanup.js`, `websearch-toggle.js`, `sidebar-auto-hide.js` (also deleted dead constant `div._70b689f` with no readers), `mobile-homepage-cleanup.js`, `preset-overlay.resolvers.js`, go-top (3 files, cross-file duplicate `_0706cde` consolidated to single constant). Thinkblock write selectors `_74c0879`/`_9ecc93a`/`ds-think-content` rebuilt from query constants to eliminate implicit duplicate literal values. 24 affected specs all green (496 tests) |
| [4.29.0](changelog/v4.md#4290---2026-08-22) | Editor window added "auto-close on page regain focus": `chat.deepseek.com` tab's window `focus` event converted to `DSS_CLOSE_EDITOR_WINDOWS` message by new `content/editor-window-autoclose.js`; new `background/editor-window-routes.js` reads `chrome.storage.session` for global/prompt-group editor window ids, removes each via `chrome.windows.remove` and deletes corresponding keys, responds `{ ok: true }` (single window already-gone rejection does not affect other id and key cleanup). Message type and two session storage keys centralized in new `utils/editor-window-constants.js`; `popup/popup.editor-window.js` now gets storage keys from the constants file with fail-fast throw on missing load; `manifest.json` `content_scripts`, `popup/popup.html`, and `background/service-worker.js` entry points wired up. Existing editor auto-save (500ms debounce, `blur`/`visibilitychange`/`pagehide` immediate write) flushes before auto-close, so no content is lost. Added `editor-window-routes.spec.js` (6) and `editor-window-autoclose.spec.js` (4); `popup-editor-window.spec.js` updated to preload constants file |
| [4.28.1](changelog/v4.md#4281---2026-08-22) | Phase 8 wave 2: settings read scope converged to allowlist, sync reads batched, dead code removed; extracted independent `utils/storage-manager.settings-read.js` bundle part with four entry points and `manifest.json` wired up. `storage-manager.sync.js` at 362 lines post-change falls in the 250–450 tolerance range — file focuses on single cohesive responsibility (sync conflict and retry: `retrySync`, `resolveSyncConflict`, `restoreSettings`) and is intentionally not split further. Added allowlist spec; five existing storage-manager and editor-html specs redirected to new part |
| [4.28.0](changelog/v4.md#4280---2026-08-22) | i18n lifecycle rewrite with MAIN world injection module extracted. `utils/i18n.js` removed autoInit IIFE; locale resolution now handled by `init()` with single protected listener registered; added `onLocaleChanged(cb)` subscription API replacing `dsI18n-locale-changed` document event; added `_dataFor` query and cleared all DOM dependencies from engine; DOM application logic moved to new `popup/popup.i18n-apply.js`. `censor-reply-restore.js` four inline MAIN world injections replaced by new `content/main-world-injector.js`; that file renamed to `_startFragmentListener`. Also retired three tech debts: `censor-reply-restore.dom.js` merged `_injectThinkContent` and `_createRestoredContainer` duplicate logic (think block selectors moved to `ds-selectors.js`); `storage-manager.chatmap.js` extracted shared `_computeChatPresetMapDiff` for locked and unlocked paths; `content-script.export.js` simplified `parseHtmlToMarkdown` with `TAG_HANDLERS` lookup table. Test environment switched to explicit `dsI18n.init()` calls |
| [4.27.0](changelog/v4.md#4270---2026-08-22) | Phase 7 wave 2: popup domain and view modules extracted with settings normalization fix. Added `popup.preset-domain.js`, `popup.settings-view.js`, `custom-select.drag.js`; `popup.js` reduced from 428 to 330 lines (new preset handling, `bindCurrentChat`, tab UUID resolution moved to `popup.preset-manager.js` 141→242 lines; locale binding renamed to `bindLocaleSwitcher`); `custom-select.js` from 411 to 247 lines; `editor.js` removed local `isDuplicateName` in favor of delegating to `createPreset` and `validatePresetName`. storage-manager series imports shared `normalizeWebsearchToggle`; `settings-routes.js` normalizes `dsWebSearchToggle` in GET route; initialization protects `pruneOrphanChatBindings` with empty index guard to preserve existing bindings. `popup-add-preset.spec.js` regex source extraction replaced by behavior tests |
| [4.26.0](changelog/v4.md#4260---2026-08-22) | All popup-layer chrome API calls extracted to shared `utils/` modules: added `tab-control.js` (`queryActiveDeepseekTab`/`queryDeepseekTabs`/`sendToTab`), `window-control.js` (true singleton window opening), and `debounce.js` (merging three duplicate implementations scattered across popup); seven popup files no longer directly call `chrome.tabs`/`chrome.windows`/`chrome.storage`. `storage-manager.local.js` added `restored_messages` series API with content-side reuse; editor window switched to session-stored window id for singleton; styles and layout consolidated with duplicate token and option naming |
| [4.25.0](changelog/v4.md#4250---2026-08-22) | Pending-delete queue now owned and operated by Service Worker message routes: added `background/pending-store-routes.js` providing `DSS_TRACK_FOR_DELETION`, `DSS_REMOVE_PENDING_DELETE`, `DSS_REMOVE_OPEN_UUID`, `DSS_SET_LAST_AUTH_TOKEN` four routes delegating to `TemporaryChatPendingStore`; `content/temporary-chat-pending-store.js` removed from `content_scripts`; all direct calls across `temporary-chat-delete` three parts converted to messages (constants 13→17). Preset overlay also retired three debts: P8 (`onSelectChange` no longer mutates ctx map in-place; persistence routed through `StorageManager` transaction path with error interception), P7 (removed defunct `reason` parameter), P11 (`storageManager`/`i18n` injected via ctx with bare-global fallback). Route spec passed red-green |
| [4.24.1](changelog/v4.md#4241---2026-08-22) | Phase 6 wave B: message route migration, module splitting, and toast i18n fix. Added `chat-binding-controller.js` (conversation-preset binding state machine extracted from `content-script.js`, 300 lines, justified at 250-line tolerance boundary as single cohesive state machine), `prompt-injector.send-button.js` (P6), `censor-reply-restore.keymap.js` (C13 reverse index, red-green verified and certified by neutral executor). `temporary-chat` series and `content-script.js` migrated from `chrome.storage` to DSS settings message routes; P4 — `preset-overlay.controller.js` removed own body observer, using `scheduleFindAndMount()` with 150ms debounce dispatched from `content-script.js` single body observer; P14 — send button switched to SVG path selector replacing `innerHTML` scanning; C11 — delete failure toast switched to i18n key `tempChatDeleteFailedToast` with `ds-` prefix class |
| [4.24.0](changelog/v4.md#4240---2026-08-22) | 9 content scripts switched to message-based toggle pipeline: `auto-retry`, `go-top`, `hide-thinking`, `mobile-homepage-cleanup`, `mobile-sidebar-swipe`, `prevent-auto-scroll-bridge`, `quote-reply`, `sidebar-auto-hide`, `websearch-toggle` now use `DSSFeatureToggle.registerFeatureToggle` with `DSS_SETTINGS_MSG` GET and broadcast for settings, no longer directly reading `chrome.storage`. `quote-reply` also adopted master switch enable/disable lifecycle, with `selectionchange` consolidated into single scheduler |
| [4.23.3](changelog/v4.md#4233---2026-08-22) | Theme B wave 1: site selectors scattered across feature modules consolidated into `content/ds-selectors.js`, covering 17 consumer endpoints with ~60 sites; two previously audit-missed sites added; `manifest.json` load order adjusted so `ds-selectors.js` loads before all consumers |
| [4.23.2](changelog/v4.md#4232---2026-08-22) | C4: four duplicate chat session id regexes replaced with shared `DSSChatSessionId` calls (`censor-reply-restore.js`/`.dom.js`, `content-script.js`, `temporary-chat-delete.tracking.js`); also renamed `censor-reply-restore` resolvers to fix collision spec-detected global name conflicts |
| [4.23.1](changelog/v4.md#4231---2026-08-22) | Extracted `content/width-feature.js` factory to unify settings reading, application, and `MutationObserver` monitoring; switched to Service Worker message routes (`GET_SETTINGS`/`SETTINGS_CHANGED`) for settings; `chat-width.js` 152→38 lines, `input-width.js` 192→65 lines, each retaining only specific settings; removed 6 direct `chrome.storage` call sites |
| [4.23.0](changelog/v4.md#4230---2026-08-22) | Built four shared content base modules for Theme C with unit tests each: `feature-toggle.js` (messaging-based feature toggle registration), `retry-until.js` (retry utility), `mobile-device.js` (`isMobileDevice` detection), `chat-session-id.js` (`extractChatSessionId` parsing); consumers migrate in subsequent versions |
| [4.22.2](changelog/v4.md#4222---2026-08-22) | C10: `harvest.js` main loop bottom detection now calls `harvest.dom.js`'s `_isAtBottom()`, making bottom tolerance a single source of truth; removed `harvest.js`'s duplicate `HARVEST_BOTTOM_TOLERANCE` definition and redundant `clientHeight` read |
| [4.22.1](changelog/v4.md#4221---2026-08-22) | Fixed constants file not loaded causing `_getConst` to always fall into fallback: `temporary-chat-constants.js` switched to `globalThis` publishing; pending-delete queue and delete API references updated accordingly. Also reorganized Service Worker structure — `performDeleteFetch` moved to new `utils/deepseek-api.js` for centralized management; merged duplicate `onAlarm` listeners; added termination condition documentation for remediation guard |
| [4.22.0](changelog/v4.md#4220---2026-08-22) | Added Service Worker settings read/write message routes and change broadcast mechanism as the foundation for consumers to stop directly accessing `chrome.storage`: added `utils/settings-message-constants.js` and `background/settings-routes.js` (broadcasts to all DeepSeek tabs after successful write); Service Worker loads `[DSS]` logger per D8 spec and executes `DSSSettingsRoutes.install()` at top level; `manifest.json` added `chat.deepseek.com` `host_permissions` and registered settings message constants. Test side extracted `popup-script-loader.js` for centralized eval loading template; `popup-live-sync.spec.js` switched to virtual time advancement replacing real waits, execution time from ~5s to ~1.5s (T3/T6) |
| [4.21.9](changelog/v4.md#4219---2026-08-22) | D7: logger prefix unified from `[DS-Sync]` to `[DSS]`; `chunk-lock`, `websearch-toggle`, `temporary-chat-pending-store` direct `console` calls converted to unified logger; `ARCHITECTURE.md` references to old prefix updated. Test side: T1 — 2,231-line `censor-reply-restore.spec.js` split by concern into seven files with shared fixtures extracted; SseParser section moved to `sse-parser.spec.js`; test count maintained at 137. Also extracted `load-classic-script.js`/`set-pathname.js`/`pending-store-mock.js` replacing duplicate boilerplate across nine specs; removed dead exports and two unreferenced stale HTML fixtures |
| [4.21.8](changelog/v4.md#4218---2026-08-22) | D2: `broadcastActivePreset()` changed from sending to active tab only to querying all `chat.deepseek.com` tabs and sending concurrently via `Promise.all` — tabs without `id` skipped, individual tab `sendMessage` rejections swallowed independently; single tab failure does not affect others or the caller; global mount falls back to `globalThis` in environments without `window`. Three docs still describing "sent to single tab" updated; multi-tab contract test added |
| [4.21.7](changelog/v4.md#4217---2026-08-22) | Split `temporary-chat-delete`: tracking, coordination, and event handling separated into part files (`.tracking.js`/`.coordinator.js`/`.handlers.js`) registered in manifest; entry file reduced to 219 lines; `temporary-chat-toggle.js` switched to shared enabled flag module and fixed outdated `chrome.storage.session` comment; enabled-flag module added `__setCache` test hook; tests switched to shared chrome mock, added real `init` test, removed tautological and split-brain workarounds |
| [4.21.6](changelog/v4.md#4216---2026-08-22) | Extracted temporary chat enabled flag caching, reading, and `onChanged` listening logic into shared module `content/temporary-chat-enabled-flag.js` registered in `content_scripts`; 22 unit tests covering behavior (red-green verified, including two subsequent consumer specs totaling 177 tests all passing) |
| [4.21.5](changelog/v4.md#4215---2026-08-22) | Fixed three feature modules' `disable()` not fully releasing resources: chat-width and input-width now disconnect `MutationObserver` and clear apply timer in `disable()`; removed write-only `styleEl`, empty `#STYLE_ID {}` CSS rule, and unused `ChatWidth.applyWidth`; `destroy()` aliased to `disable()`. sidebar-auto-hide now uses per-enable-cycle `AbortController` for hover listeners aborted in `disable()`, preventing listener accumulation on repeated toggle; `disable()` also disconnects both observers and clears resize timer; body observer created in `enable()`. auto-retry: fixed comment constant name to `RESIZE_DEBOUNCE_MS` |
| [4.21.4](changelog/v4.md#4214---2026-08-22) | Consolidated go-top observers and fixed attribute change storm performance issue: removed `attributes: true` from body `MutationObserver` to avoid triggering callback on every attribute change (including module's own `style.display` writes) on React pages; deleted second body subtree route observer, route detection merged into existing 50ms debounce observer with `popstate` listener added; removed write-only `isInjected`. Tests replaced defunct `_routeObserver` field assertions with route-change behavior tests (`popstate` re-injection, body mutation re-injection, no injection after disable) |
| [4.21.3](changelog/v4.md#4213---2026-08-22) | Reduced preset overlay settlement loop polling cost: convergence parameters `maxFrames` 7200, `stableK` 120 reduced to 60 and 4, shortening post-mount forced layout polling from "2 seconds to 2 minutes" to under 1 second (typical ~83ms); `ResizeObserver` and window resize listener take over after convergence; added 60-frame limit assertion test |
| [4.21.2](changelog/v4.md#4212---2026-08-22) | Removed write-only `pendingStates` Map from `censor-xhr-hook.js` — dead code continuously accumulating `XMLHttpRequest` references causing memory leak; removal does not change execution behavior |
| [4.21.1](changelog/v4.md#4211---2026-08-18) | Timestamp and prompt injection now covers pure attachment/image send scenarios: previously empty textarea meant no injection at all, missing timestamp and prompt prefix on pure attachment/image messages. `injectPrefix(textarea, isSendableWithoutText)` added second parameter; when textarea is empty but sendable, injection still occurs with output omitting `<user-input>` wrapper, keeping only timestamp and prompt prefix. "Is sendable" reads DeepSeek send button's own state (`ds-button--disabled`/`aria-disabled`/`disabled` attribute); added `isSendButtonEnabled()`/`findSendButtonForTextarea()` using language-agnostic structural detection, not rotating hash classes. Click path textarea resolution changed to three-tier priority: walk-up non-empty textarea preferred, then global `document.querySelector('textarea')` fallback (if non-empty), empty textarea only when both yield no non-empty textarea (walk-up preferred, otherwise global fallback); Enter path wired to same send button detection; `markChatCreationAttempt()` covers this scenario. 7+ new test cases added, target five specs all green |
| [4.20.4](changelog/v4.md#4204---2026-08-17) | Web search button locate failure warning changed from "warn on every attempt" to "single warn after give-up deadline expires". User testing reported two `failed to locate` warnings (one from `start()`, one from observer) while feature actually worked — `start()` runs synchronously at content script injection when React toolbar hasn't mounted yet, and observer fires unfilttered/undebounced on any `document.body` change; by third trigger toolbar was present and successfully applied. Both were stale false alarms; icon prefix `M7.9995999336` and two-tier locator logic verified correct, unchanged. Changed to `LOCATE_GIVE_UP_MS` (15000): `enable()` deploys deadline when not located; fires once when expired and `_isSpent` still false (message unchanged); successful application `clearTimeout` in same synchronous branch as `_isSpent` turning true; `_armGiveUp()` cancels before scheduling; `disable()` and `_rearm()` unconditionally cancel; three paths leave no dangling timers. False alarms zeroed; real failures still produce one clear signal. Test side: four direct `findButton()` cases changed to "returns null without warning"; 7 new cases covering three no-immediate-warn paths, deadline-expired exactly-once, and user-scenario reproduction zero-warning core case; core case initially flaky (third run `btn.click` got 0 calls), root cause: fake timer `MutationObserver` microtask callbacks may be entirely dropped; fixed by switching to real timers only for the delivery-wait step; all three assertions retained unweakened, five consecutive certifications 42/42, full suite 2078 all green |
| [4.20.3](changelog/v4.md#4203---2026-08-17) | Fixed diagnostic log silently dropping error objects. `logWriteFailure` called `__DS_Logger.warn('pending-store:write-fail', context, error)` with three arguments, but `warn(event, data)` declares only two parameters — the third argument (actual `Error`) was silently dropped by JavaScript, never reaching `console.warn` — production testing showed only operation names with zero error content, making underlying storage failures completely undiagnosable. Fixed to `warn(event, ...rest)` spread forwarding; `rest` empty spreads `['']` to maintain original behavior of not printing literal `undefined`; affects 6 call sites, all fixed in single logger location. Test environment patched: `vitest.setup.js` originally did not preload `utils/logger.js`; `logWriteFailure` always took the variadic fallback branch, structurally untestable; Group F originally only asserted `warnSpy` was called, strengthened to assert `Error` instance actually appears in arguments; also added previously zero-coverage `removeOpenUuid` failure path. Root cause trigger: reloading extension then reusing old tab creates orphaned content script (`chrome.storage` all throws); tab refresh resolves; write logic itself unchanged |
| [4.20.2](changelog/v4.md#4202---2026-08-17) | Fixed editor window "Send" click no longer re-injecting prompt (Enter still worked). `isEditSendButton` originally identified editor send button by button label text equaling literal string `发送`; on Traditional Chinese (actual label "傳送"), English, and other locales this was always `false`, and `isSendButton`'s remaining conditions (`M8.3125` icon prefix, `.ba4f09d3`, `.bf38813a`) all belonged to main textarea send button markers, so handler directly returned and click passed through; Enter path only checks focused textarea, not button markers, hence unaffected. Changed to named predicate `isEditWindowSendButton()`: requires both `ds-button--primary` and `ds-button--filled` (named constant `EDIT_SEND_BUTTON_VARIANT_CLASSES`) with non-empty `span.ds-button__content`. Cancel button is outlined variant, rejected by first guard; main textarea send button is also primary/filled but pure icon with no content span, falling back to existing main textarea detection branch. No label text, no rotating hash classes, no icon geometry (these two buttons have no SVG). This defect existed since file creation (`42f6fcf`), unrelated to 4.20.0/4.20.1 — 4.20.1 fixed a same-class bug at a different site. Test side: TC-4/TC-5 originally asserted `发送` literal comparison as correct contract, effectively locking in defect; TC-4 changed to three-locale `it.each`; TC-5 cancel button fixture originally only changed label text with structure identical to send button (passed only because "取消 ≠ 发送"); rebuilt with real outlined markup; 13 tests all green |
| [4.20.1](changelog/v4.md#4201---2026-08-17) | Fixed web search toggle silently failing on non-Simplified-Chinese interfaces: button locating no longer depends on label text; changed to language-agnostic two-tier locator — search icon SVG `path[d]` prefix (`SEARCH_ICON_PATH_PREFIX`) primary, toggle group position fallback; two failure modes mutually exclusive; `console.warn` once on complete failure for diagnostics. Generic `[aria-pressed]` selector retained as icon-tier candidate source. Tests entirely rebuilt with language-agnostic fixtures; 35 tests all green |
| [4.20.0](changelog/v4.md#4200---2026-08-16) | Global prompt toggle changed from single device setting to per-preset independent property: prompt group A on, B off means messages sent with A carry global prompt while B does not. Flag stored in `PromptPreset.globalPromptEnabled`; syncs cross-device with preset; missing field defaults to `true`, so upgrade is zero behavior change for existing users. Content (`globalDefaultPrompt`) remains single shared string — only the toggle is per-preset. Device-level key of same name retained but demoted to fallback, used only when no active preset exists (blank option, unbound conversation); resolution logic centralized in single pure function `resolveGlobalPromptEnabled()` shared by popup and content script. No new UI components; reuses existing `#globalPromptToggle`. Also fixed two defects: SPA navigation doesn't trigger `chrome.storage.onChanged`, so switching from a conversation bound to an "off" preset to an unbound conversation left global prompt silently suppressed until popup reopened — fixed by having `handleChatChange()` recompute in every branch; effective id resolved by existing `resolveOverlayPresetId()` instead of parallel implementation; deleting a preset emptying `activePresetId` without re-rendering toggle fixed by zero-parameter ctx hook. UI text unified from "全域預設提示詞" (Global Default Prompt) to "全域提示詞" (Global Prompt) to avoid confusion with v4.18.0's "預設提示詞組" (Default Prompt Group / pin). Test side fixed a real race condition: `chrome-storage-mock`'s `onChanged` dispatches synchronously without replay, and `addListener` sits at the end of un-`await`ed bootstrap; fixed-tick-count `flush()` didn't work; changed to deterministic completion signal polling |
| [4.19.1](changelog/v4.md#4191---2026-08-08) | Scroll step size changed to per-step measured derivation, replacing fixed 0.9× viewport height. Field measurement of two real conversations established virtual list mounts fixed 18 nodes (regardless of message height), does not extend upward, extends downward 3406–4244px — fixed pixel step therefore gambles on message length; short-message segments scroll past never-mounted content causing permanent loss. Changed to measuring "distance from lowest mounted node bottom to container visible top" per round, taking 70% as step (minimum 25% viewport height; unmeasurable falls back to 90%); long-message segments auto-scale step to 3–4 viewports, short ones shrink; field sample step increased from 889px to 3620/3034px, ~3.4–4.1× fewer steps. Key-sequence gap-based completeness proof rejected (sample 1 distinctDiffs = [1,3], gaps exist naturally). `harvest.js` split out `harvest.dom.js` (446→303 + 224 lines); entry file re-exports every moved function under original name; 58 existing tests unchanged. Test coverage filled structural blind spot: existing `scrollBy` mock hardcoded 0.9 ignoring actual arguments, and happy-dom `getBoundingClientRect` returns all 0s causing all tests to only exercise fallback path; now asserts precise step values (3620/3034) with red-light sharpness proving new assertions catch old implementation |
| [4.19.0](changelog/v4.md#4190---2026-08-08) | Fixed long conversation Markdown export cutting off unexpectedly, producing truncated files, and added export cancel. Deleted `HARVEST_TOTAL_TIMEOUT` (120000ms) hard time limit — budget exhausted by idle waiting not loading (per-step DOM stability wait minimum cost 3 × 150ms = 450ms paid even with no new content); total scrollable distance thus hard-limited to ~240 viewport heights regardless of conversation length; "export cut off" and "export too slow" are two sides of the same defect. Changed to 20 consecutive seconds without progress (`HARVEST_STALL_TIMEOUT_MS`) as sole time-based stop condition; never terminates while progress continues; `scrollHeight` change in either direction counts as progress since virtual list can shrink. Pure decision logic extracted to `content/harvest.policy.js` (clock injected via `nowMs`, zero DOM); 29 tests need no fixtures or fake timers, also resolving `harvest.js` approaching 450-line threshold. Footer warning changed from `describeIncompleteReason()` to report actual cause (previously all five interruption reasons hardcoded as `timed out`) with captured count; also added visible on-page warning toast — previously warning existed only in file footer, in field test found at line 12209 of 12209; user experience was "completely no indication". Added cancel button using native `AbortController`; cancellation still exports captured portion. `HARVEST_STABLE_INTERVAL` 150→100ms (per-step minimum 450→300ms), but `HARVEST_STABLE_TICKS` intentionally kept at 3: premature convergence scrolls past content while rendering, permanently losing messages. Also fixed a preexisting defect: `manifest.json` carried UTF-8 BOM since v4.18.1 (`d288574`), causing `content-script-global-collisions.spec.js`'s `JSON.parse` to throw; that guard test had been red ever since |
| [4.18.2](changelog/v4.md#4182---2026-08-07) | Editor window preset name input now only focuses without selecting content: removed `nameInputEl.select()` following `focus()` in `popup/editor/editor.js`; cursor lands at end of existing name; pressing keys no longer clears entire old name at once. `popup/popup.modal.js` new/rename modal `focus()` + `select()` intentionally retained (input often empty or needs full replacement) |
| [4.18.1](changelog/v4.md#4181---2026-08-07) | Fixed three defects in the in-page preset dropdown. **Entry expansion jump**: `mountTo()` appended element without inline `left`/`width`; first `reposition()` landed in next animation frame; browser drew one unpositioned-geometry frame; no CSS transition was ever involved; extracted `_applyPlacementSync()` to apply once synchronously in same task as `appendChild`; settle loop constants and frame count unchanged; intentionally no fade-in mask. **Width stuck at 200px**: measurement only read current selected label's `scrollWidth`, never iterating options; once long, stuck at `maxWidth` ceiling; changed to all option names plus placeholder as candidate set fed to new pure function `pickNaturalWidth()` taking maximum; function intentionally sets no upper limit (clamping to available space remains `computePlacement()`'s job); instance cache avoids per-settle-frame reflow. Native fit lost when `d560729` switched to custom combobox. **In-page switch to new conversation not showing pinned default**: `findAndMount()`'s hardcoded ternary returned literal `''` when no chat id; React replacing title bar then deterministically overwrote just-pushed value; decision extracted to pure function `resolveOverlayPresetId()`; pending id changed to three-value (valid id, `''` meaning explicitly chose blank — no fallback, `null` meaning not yet chosen — apply default); two `id \|\| null` changed to `??` — `'' \|\| null` being `null` was exactly why explicit choice was overridden by default. Also split per §8 threshold into `preset-viewport-sync.js` and four `preset-dropdown.*` modules |
| [4.18.0](changelog/v4.md#4180---2026-08-07) | Dropdown list now shows a pin button left of `✕` on each row; pinning a preset makes it the "default" auto-selected for new conversations. Default stored as single scalar key `pinnedPresetId`, so "only one at a time" is structurally inevitable rather than validation-dependent; effective point is `handleChatChange()`'s no-UUID branch, which only enters when URL has no chat id, so "switching to existing conversation is always unaffected" needs no additional guard. Toggle logic isolated to `popup/popup.pin-manager.js` (`popup.js` already over 450-line threshold; only wiring added); deleting a pinned preset also clears the default; `pinnedPresetId` included in settings backup export and import. Also fixed two defects: row button `display` toggle causing pin shift on hover enter/exit, and hovering directly over lit pin causing highlight disappearance |
| [4.17.1](changelog/v4.md#4171---2026-08-06) | Fixed changing web search setting in popup having no effect on already-open pages (requires refresh). v4.17.0's one-shot model only had "content script startup" as the single apply timing; once `_isSpent` was consumed it never reset, so `storage.onChanged` updated in-memory `mode` but `_recompute()` fell into `disable()` which only stops observer without touching the button. Added `_rearm()` (sequential `disable()`, reset `_isSpent`, `_recompute()`); both branches of `setupStorageListener` now call it; apply timings expanded from one to three: entry, setting change, master switch off-to-on. Each event still applies only once then fully hands off; one-shot semantics not weakened; master switch off-to-on path also fixed, behavior consistent with other content modules |
| [4.17.0](changelog/v4.md#4170---2026-08-06) | Injection wrapper tag `<system-prompt>` renamed to `<system-reminder>` (`<user-input>` inner tag intentionally unchanged, so edit-message unwrap regex needs no modification); web search changed from three-state forced to two-state one-shot entry default: `dsWebSearchToggle` reduced to `'on'` \| `'off'` (default `'on'`; residual old `'default'` corrected to `'on'` on read without write-back); popup removed "Default" option and `websearchDefaultLabel` i18n key. `content/websearch-toggle.js` uses `_isSpent` flag to apply setting only once per page load, disconnecting observer after application; removed button `aria-pressed` attribute observation and `CLICK_COOLDOWN_MS` throttle; user manual toggle of web search result preserved until leaving or refreshing, no longer reverted |
| [4.16.4](changelog/v4.md#4164---2026-08-02) | Fixed sync reconciliation writing back non-owned keys, causing temporary chat toggle to revive after refresh: `resolveSyncConflict()` spread `{ ...localRaw, ...syncRaw }` merging two complete unfiltered snapshots then wrote back wholesale; any key not in `StorageManager.KEYS` hitchhiked; stale cloud `true` thus overwrote user's just-written local `false` and self-perpetuated. Fixed by rebuilding payload using `KEYS` dynamically-exported ownership allowlist (including `dsPreset_` prefix); intentionally not deleting existing non-owned keys in sync since `utils/i18n.js` locale setting also falls outside `KEYS` — deleting would destroy user preference |
| [4.16.3](changelog/v4.md#4163---2026-08-02) | Fixed temporary chat toggle reverting to on after refresh: field probe confirmed turning off writes `false`, but after refresh the key becomes `true`, meaning a real write occurred during refresh; only write path across entire repo is the checkbox's own `change` listener with no synthetic events; determined to be Chromium's state restoration for dynamically injected form controls firing `isTrusted` `change` event misidentified as user action; added `autocomplete="off"` to opt out of restoration. Also fixed `writeEnabledFlag()`'s `chrome.storage.local.set()` being fire-and-forget with outer synchronous `try/catch` unable to catch promise rejection, silently swallowing write failures |
| [4.16.2](changelog/v4.md#4162---2026-08-02) | Web search label and three-segment control changed to same row: `.input-group:has(> .websearch-options)` structural selector overrides group to flex row with right-aligned vertical centering; other groups in same card unaffected; no new classes; `popup.html` zero changes |
| [4.16.1](changelog/v4.md#4161---2026-08-02) | Popup visual refresh round 2: checkboxes and radio buttons changed to `appearance: none` custom-drawn (box/circle, checked state is light blue background with blue check/dot, all with hover, focus ring, and disabled state); web search three options changed to single bordered segmented control with tightened padding and font size; restored first-round accidentally deleted icon button hairline border. Pure CSS; DOM and class names unchanged |
| [4.16.0](changelog/v4.md#4160---2026-08-02) | Popup visual refresh: added `popup/popup-theme.css` as sole design token source (brand blue `#4d6bfe` unchanged; also defines surface layers, three-tier text, two-tier borders, three-tier shadows, border radius and spacing scales); four stylesheets converted to all-token colors with zero hardcoded hex codes; first dark mode support via `@media (prefers-color-scheme: dark)` overriding same tokens (no manual toggle, no JS, no new storage key). Visually: cards changed to hairline border with soft shadow and larger border radius; title hierarchy consolidated; controls unified with border radius and focus ring; dropdown selected item changed from solid blue bar to light blue background with blue text. Pure style change: no selectors added or renamed; `popup.html` only one additional `<link>` |
| [4.15.1](changelog/v4.md#4151---2026-08-01) | Fixed temporary chat being deleted when another tab opens a new conversation: open-conversation guard originally used single array key `dss-open-temp-uuids` with read-modify-write on every add/remove; any context reading a stale snapshot silently dropped other tabs' items; conversation lost its guard and was then deleted by Service Worker remediation scan (field probe confirmed tab A's item landed, then seconds later erased by tab B's write-back). Changed to per-UUID independent key (prefix `dss-open-temp-uuid:`); add writes only own key, remove deletes only own key; this class of loss structurally impossible. Old array key converted to read-only with union deduplication on reads; `clearOpenUuids()` also clears it. Also removed now-purposeless `withOpenUuidsLock` and its asymmetric 1000ms/5000ms timeout causing orphan lock hazard; `utils/storage-manager.chunk-lock.js` TTL lock generalized to configurable key while preserving original thin wrapper |
| [4.15.0](changelog/v4.md#4150---2026-08-01) | Standalone editor window added `Esc` close shortcut: window-level `keydown` listener detects `Escape` and calls `window.close()`; existing `pagehide` auto-save writes unsaved content before closing; shortcut close loses no data |
| [4.14.0](changelog/v4.md#4140---2026-08-01) | Prompt group renaming moved to editor window: removed pencil button from dropdown list; renaming now done in auto-focused name input at the top of standalone editor window, sharing same auto-save pipeline (500ms debounce + blur/close flush); duplicate names rejected on save with red error displayed in save status area. Re-clicking pencil when editor already open reloads its tab (`chrome.tabs.update`), re-focusing name input each time; `pagehide` auto-save guarantees no data loss. Popup dropdown reflects new name automatically via existing live-sync; popup side zero changes |
| [4.13.0](changelog/v4.md#4130---2026-08-01) | Added "Web Search" three-state setting: popup "UI Adjustments" card added radio group ("Default" / "On" / "Off"), storage key `dsWebSearchToggle` (string, default `'default'`). "On" clicks page's web search button to maintain `aria-pressed="true"`, "Off" maintains `"false"`, "Default" does not interfere; **only clicks when state mismatches** (clicking when matching would toggle it off). Implemented in `content/websearch-toggle.js` (following `hide-thinking.js` pattern: `chrome.storage.onChanged` + single `MutationObserver` with dual targets — body childList/subtree for SPA re-render, button attributes for in-place flip; `CLICK_COOLDOWN_MS` 500ms prevents click ping-pong; linked to master switch). Element identification: live page has two identical `.ds-toggle-button[aria-pressed]` (deep thinking and web search); `querySelector` gets first; identified by label text containing "搜索"; retains generic `[aria-pressed]` fallback |
| [4.12.0](changelog/v4.md#4120---2026-08-01) | Added "Prevent Auto-Scroll" toggle: previously the anti-scroll-back protection only engaged briefly during "Back to Top" and Markdown export; users can now set it to persistent. Persistent mode implemented in shared throttle point `prevent-auto-scroll-bridge.js` (added `setPersistent()` / `isPersistent()` / `start()`; state stored in existing hidden bridge element's `dataset`); `harvest.js` and `go-top.scroll.js` callers unchanged. `disable()` becomes no-op in persistent mode — otherwise the flag has no reference count and `harvest.js`'s unconditional `disable()` in `finally` would silently turn off user-enabled protection after every export. Linked to master switch; supports instant toggle without refresh. Trade-off: existing patch is `Element.prototype`-level global interception that cannot distinguish programmatic from user-initiated scrolls; when persistent, streaming reply auto-follow scrolling is also blocked (native scroll wheel unaffected); therefore off by default |
| [4.11.19](changelog/v4.md#41119---2026-07-26) | Fixed preset ordering being reshuffled on timestamp tie, causing drag results to disappear on next open: `mergePresets()` originally only adopted corresponding side's `order` when timestamp was strictly newer; on tie it fell back to merged Map insertion order (locally cached presets floated to front), making visible order depend on which objects happened to be cached in local rather than saved order array; since `_set()` mirrors same object into both storage areas, ties are actually the norm. Fixed to adopt cloud `order` on tie (only self-converging choice). Also fixed `STORAGE.md` still describing `#forceSyncBtn` removed in v4.8.5, and two docs still claiming `dsLocalAuth` affects read priority (removed in v4.7.2) |
| [4.11.18](changelog/v4.md#41118---2026-07-26) | Fixed cross-device sync order rollback and "deleted preset resurrection": `retrySync()` pending-push loop blindly pushed `dsPresetOrderMeta` and `dsPresetTombstones` (former didn't match any existing guard due to `startsWith('dsPreset_')` trailing underscore); stale local values overwrote newer cloud values. Order metadata now uses same `>=` comparison as `PRESET_INDEX`; tombstones changed to per-id union merge (reusing existing `_mergeTombstones()`); empty local set no longer clears cloud |
| 4.11.17 | Fixed top-level identifier collision causing `go-top.js` to fail entirely and "Back to Top" button to disappear: v4.11.13 introduced shared selector module causing `harvest.js` and `go-top.js` to each declare `const __DSSelectors` at top level; classic scripts share same global scope so later-loaded file throws `SyntaxError: Identifier '__DSSelectors' has already been declared`; changed to unique names `__DSSelectorsHarvest`/`__DSSelectorsGoTop`; added static guard test based on manifest content_scripts list (this class of defect structurally uncatchable by unit tests — vitest runs each file in isolated scope) |
| 4.11.16 | Split popup.js: editor window, width sliders, Markdown export each independent as method bundles (popup.editor-window.js/popup.width-sliders.js/popup.markdown-export.js); popup.js from 572 to 441 lines clearing 450-line threshold; two specs using regex source extraction only changed read paths |
| 4.11.15 | Split content-script.js: prompt injection extracted to prompt-injector.controller.js, from 491 to 360 lines; chose injection side over state side to avoid breaking initSettings regex assertions |
| 4.11.14 | Split i18n.js: translation dictionary extracted to pure data file i18n.locales.js; engine from 501 to 167 lines; four entry points (manifest, popup.html, editor.html, vitest.setup.js) all require data file before engine |
| 4.11.13 | Centralized duplicate DeepSeek selector constants to ds-selectors.js (mounted as `window.DSstudio.Selectors`); six reference points switched to shared constants; selector strings remain byte-identical; two walk-up functions unchanged |
| 4.11.12 | Split censor-reply-restore.dom.js: think block widget extracted to censor-reply-restore.thinkblock.js, from 489 to 396 lines |
| 4.11.11 | Eliminated duplicate key-diff application logic in storage-manager.chatmap.js: non-locked fast path and locked slow path duplicate three-step key-diff application extracted to shared helper `_applyChatPresetMapDiff`, from 456 to 425 lines |
| 4.11.10 | Split popup.modal.js: Toast component extracted to popup.toast.js (separation-of-concerns split, not line-count threshold); both files use Object.assign to mount independently, no longer dependent on script tag order |
| 4.11.9 | Split harvest.js: progress toast UI extracted to harvest.toast.js; harvest.js from 502 to 426 lines clearing 450-line threshold; manifest inserts new file before harvest.js to ensure load order |
| [4.11.8](changelog/v4.md#4118---2026-07-26) | "Back to Top" changed to instant: each round replaced `scrollBy(0, -0.9 * viewportHeight)` incremental scroll with direct `scrollTop = 0` write; arrival time no longer proportional to conversation length (50 viewport-height conversation from ~5.9s to instant); convergence gate, timeout, abort, and `PreventAutoScroll` coordination all preserved; removed unreferenced `SCROLL_STEP_FACTOR`; first explicit documentation of intentional asymmetry "up: instant, down: incremental" |
| [4.11.7](changelog/v4.md#4117---2026-07-26) | Fixed Markdown export silent truncation: `_scrollToTopAndSettle()` discarded `scrollToTopAndWait` return value; scroll-to-top failure (timeout or user pressing "Back to Top" mid-scroll) still started capture from current position; output silently missing oldest messages with no error indication. Fixed to propagate and check result; on failure returns `reason` as-is and aborts; teardown uses existing `finally` |
| [4.11.6](changelog/v4.md#4116---2026-07-26) | Fixed "Back to Top" scrolling up then bouncing back, never reaching top: `scrollToTopAndWait()` did not coordinate with `PreventAutoScroll`; page's own downward scroll was unsuppressed; convergence condition `currentScrollTop <= 0` was perpetually false; `_stableTopCount` reset to zero each round, spinning until 30s timeout. Fixed by saving pre-enable state at entry point and restoring in `cleanup()`; only disables when current call was the enabler, avoiding clobbering `harvest.js` nested call's protection |
| [4.11.5](changelog/v4.md#4115---2026-07-26) | Fixed GoToTop post-disable timer residual: `_onRouteChange()`'s 100ms timer handle was not saved; disabling within 100ms of route change would re-inject button and restart observer and scroll listeners after disable. Added `_routeChangeTimer` field, `_tryConnectDom()` entry guard; `disable()` now actually calls `_scrollReject` to abort in-progress scrolls |
| [4.11.4](changelog/v4.md#4114---2026-07-26) | Audit slimming batch D (native API rewrites): quote-reply stylesheet changed to static CSS file; preset-dropdown uses native `scrollWidth` replacing hand-written offscreen measurement probe. AbortController and native `<dialog>` rejected after review (happy-dom 16.8.1 silently ignores `signal` option; `HTMLDialogElement` is empty stub) |
| [4.11.3](changelog/v4.md#4113---2026-07-26) | Audit slimming batch C (structural merges): utils/ from 13 to 10 files; fixed background sync retry silently failing (importScripts never loaded tombstones.js; `retrySync()` path always threw `_mergeTombstones is not a function` swallowed by empty catch); added loader-contract guard test |
| [4.11.2](changelog/v4.md#4112---2026-07-25) | Audit slimming batch A2: removed two legacy migration paths (v1.2.x promptPrefix migration, OLD_PENDING_LOCAL_KEY; user confirmed acceptable data loss); cleared repo-wide zero-caller symbols and popup-utils.js shadow copy |
| [4.11.1](changelog/v4.md#4111---2026-07-25) | Audit slimming batch A1: repo-wide zero-reference dead code removal (export overlay, translation keys, unused fields and constants); zero behavior change; 1745 test baseline unchanged |
| [4.11.0](changelog/v4.md#4110---2026-07-25) | Added auto-retry feature: when DeepSeek response failure shows retry button, automatically clicks it every 1000ms; controlled by master switch only, no independent toggle or storage key |
| 4.10.5 | Fixed temporary chat not deleted when navigating to external URL: beforeunload suppression flag was armed by same-URL SPA push and never consumed; cross-origin navigation's keepalive delete was suppressed. Fixed to arm flag only on true full-page refresh (reload/keyboard refresh) |
| 4.10.4 | Temporary chat toggle row now shows/hides with master switch: added _masterEnabled flag and __setMasterEnabled; storage.onChanged listens for IS_ENABLED changes |
| 4.10.3 | Fixed editing message then switching to "No Prompt Group" still injecting old prompt: `updatePromptPrefixFromBinding()` now fully determined by `chatPresetMap` when conversation has currentChatUuid; no longer falls back to residual `pendingPresetId` |
| [4.10.2](changelog/v4.md#4102---2026-07-12) | Fixed tombstone merge algorithm: `clearPresetTombstones()` key deletion had no timestamp for arbitration; clear always lost to stale delete records. Tombstone entry shape changed to `{ ts, deleted }`; clear now writes `deleted:false` instead of deleting key |
| [4.10.1](changelog/v4.md#4101---2026-07-12) | Fixed deleting all presets then importing JSON backup having them re-deleted on next cross-device sync by stale tombstones; `restoreSettings()` now calls `clearPresetTombstones()` for corresponding IDs after import |
| [4.10.0](changelog/v4.md#4100---2026-07-12) | Preset list added pencil/delete hover tooltips and fixed pencil icon direction; added one-click "delete all presets" button on "(No Prompt Group)" row with confirmation dialog; `custom-select.js` split out `preset-item-renderer.js` |
| [4.9.1](changelog/v4.md#491---2026-07-11) | Fixed temporary chat "same-conversation navigation" false delete: detection changed to destination `/a/chat/s/{uuid}` UUID comparison against tracked conversations (replacing full URL string equality); navigating to same conversation with different query/hash no longer triggers false delete; refresh and off-site navigation behavior unchanged |
| [4.9.0](changelog/v4.md#490---2026-07-11) | Temporary chat deletion mechanism layered: content script direct `fetch(keepalive)` for immediate deletion (removed unreliable SW IPC relay); SW `onStartup` supplemental deletion; pending-delete queue changed to `chrome.storage.sync` as single source of truth for cross-device supplemental deletion; added Sync-Change Safeguard and locally-open conversation list to prevent false deletes; authToken stored locally only, never synced |
| [4.8.5](changelog/v4.md#485---2026-07-11) | **This version number shipped twice**: `d75bb132` (2026-06-22) added MAIN world history interception mechanism (content/temporary-chat-history-hook.js, Navigation API fallback navigation detection) and fixed init race condition; `60068776` (2026-07-11) removed popup manual sync button, simplifying to pure auto-sync. v4.md entry records only the second; first content noted here for completeness (details in git d75bb132) |
| [4.8.4](changelog/v4.md#484---2026-07-11) | Removed diagnostic-only log forwarding subsystem (logger.js sync mechanism, orphan debug file diagnostic-sidebar-log.js, temp-chat series debug logs); retained warning-class console.warn/error; no user-visible behavior change |
| [4.8.3](changelog/v4.md#483---2026-07-11) | Added preset deletion tombstone mechanism, fixing "deleted presets resurrecting" on cross-device sync; also fixed gap where sync-winning index was not persisted locally |
| [4.8.2](changelog/v4.md#482---2026-07-11) | 8KB payload guard: `_set()` splits per-key by UTF-8 byte size; oversized keys redirected to local before sync call and registered under new key `dsOversizedKeys` (self-healing); no longer entering `dsLocalAuth` endless retry. `_byteLen()` changed to TextEncoder for precise counting; `refreshSyncStatus()` added "Too Large — Local Only" state |
| [4.8.1](changelog/v4.md#481---2026-07-11) | Debounce aligned to 500ms: editor auto-save 600→500ms; chatWidth/inputWidth slider change writes now go through debounced wrapper, reducing chrome.storage write pressure |
| [4.8.0](changelog/v4.md#480---2026-07-11) | Added popup live sync (popup.live-sync.js): popup monitors chrome.storage.onChanged while open; cross-device/cross-tab changes reflected to UI in real time; no longer requires close-reopen |
| [4.7.4](changelog/v4.md#474---2026-07-11) | Fixed `_installChunkCacheInvalidator()` closure regression after splitting: listener used bare StorageManager reference instead of this; in multi-tab scenarios both listeners modified same shared instance's cache; loser always read stale chunks. Fixed by capturing `const self = this` at install time |
| [4.7.2](changelog/v4.md#472---2026-07-11) | Removed `_get()` pin-on-read override: keys pinned in dsLocalAuth no longer let stale local values permanently shadow newer cloud edits; dsLocalAuth semantics converged to pure pending-push queue |
| [4.7.1](changelog/v4.md#471---2026-07-11) | Added `StorageManager.syncNow()` unified sync entry point (popup open and content script load both call it); fixed sync-winning value not written back to local |
| [4.6.5](changelog/v4.md#465---2026-07-10) | Fixed prompt content cross-device sync failure and strengthened sync resilience |
| [4.6.4](changelog/v4.md#464---2026-07-10) | Fixed sync convergence where newer edits were overwritten by older versions |
| [4.6.3](changelog/v4.md#463---2026-07-06) | Added unified diagnostic log output to Service Worker console |
| [4.6.2](changelog/v4.md#462---2026-06-28) | Fixed cross-device cloud sync: preset order (dsPresetOrderMeta timestamps) and content (dsLocalAuth precise pinning) now sync correctly; initialization conflict detection changed to auto/manual classification; manual sync changed to push+pull; chatmap module independently split |
| [4.6.1](changelog/v4.md#461---2026-06-21) | Fixed mobile edit-message send button textarea parsing order |
| [4.6.0](changelog/v4.md#460---2026-06-21) | Integrated React Fiber native conversation deletion mechanism |
| 4.5.5 | Fixed temporary chat deletion race caused by navigation timing: when co-occurrence completes while already on conversation page, UUID now tracked immediately; avoids SPA navigation event preceding co-occurrence completion and missing tracking |
| 4.5.4 | Fixed three temporary chat defects: switched to chrome.storage.local (content script cannot access session storage); added handleToggleChanged to sync _enabledFlagCache; removed premature tryInject call from navigate event |
| 4.5.3 | Added debug logs to trace temporary chat toggle reverting to OFF after returning to homepage (tracking _enabledFlagCache change path; diagnostic only, no behavior change) |
| 4.5.2 | Patched five temporary chat requirement gaps: SW keepalive delete requests with alarms retry (max 3 attempts, every 30s), §3 cross-tab sync, §6 1000ms dual-API co-occurrence window, §9 navigation delete retry with toast, §10 beforeunload routing to Service Worker |
| [4.5.1](changelog/v4.md#451---2026-06-18) | Fixed "Temporary Chat": only deletes conversations created via create API; historical conversations never deleted; toggle removed when leaving homepage, re-injected on return; navigating to or refreshing current URL does not delete; turning off toggle still deletes already-marked conversations |
| [4.5.0](changelog/v4.md#450---2026-06-18) | Added "Temporary Chat" feature: homepage toggle control; when enabled, leaving conversation auto-calls delete API (refresh/navigating to current URL does not delete); state stored in sessionStorage |
| 4.4.2 | Fixed refresh detection: changed to semantically correct `navigationType === 'reload'` (replacing URL equality comparison) and removed else clause so keyboard shortcut listeners (F5/Ctrl+R/Cmd+R) and Navigation API work in parallel |
| 4.4.1 | POC: added off-site deletion trigger (beforeunload + fetch keepalive); Navigation API detects refresh (same-URL navigation) to avoid false delete |
| 4.4.0 | POC: captured auth token (XHR hook intercepts authorization header, postMessage relay) and calls delete API when leaving chat |
| 4.3.8 | Fixed originalWidth being incorrectly captured as collapsed width causing sidebar to not push adjacent elements when expanding: storeOriginalWidth() dual guard (skip when collapsed, don't overwrite when ≤60px); expand() invalid originalWidth safety net |
| 4.3.7 | Fixed sidebar-auto-hide overflow clipping on Edge browser: applyOverflow() applies overflow:hidden only when sidebar collapsed; avoids re-clipping during expand transition |
| 4.3.6 | Fixed three regressions: editor.html missing i18n.js script tag (dsI18n ReferenceError), broadcastActivePreset window query scope, pendingPresetId condition too strict, edit send button textarea traversal fallback (React portal) |
| 4.3.5 | Fixed locale switch not updating dropdown "None" option in real time (updateLocale() using wrong DOM selector) |
| 4.3.4 | Added version bump missed in 4.3.3 (overwrites 7b3c245 optionData sync fix: dropdown "None" option real-time switching now syncs optionData array update) |
| 4.3.3 | Implemented real-time locale switching: i18n core added chrome.storage.onChanged listener; preset dropdown added public `updateLocale()` method; quote-reply button and overlay controller respond to locale changes in real time |
| 4.3.2 | Implemented full i18n internationalization system: Chinese/English translation data (_locales/messages.json), language switching mechanism, popup and content script fully adopted `dsI18n.t()` queries, manifest added default_locale and __MSG_*. **Note**: this version number was accidentally reverted to 4.3.1 in commit `0d357e8` (commit message did not mention version change); next commit `34c68fd` restored i18n registration and recovered 4.3.2 |
| 4.3.1 | Restructured documentation: streamlined README, added FEATURES.md, corrected ARCHITECTURE.md file tree and cross-doc references (ARIA section moved to content-navigation.md etc.). **Note**: this version number shipped twice — `16f2e80c` (doc restructuring) and `0d357e8` (manifest accidentally reverted from 4.3.2) |
| [4.3.0](changelog/v4.md#430---2026-06-14) | System time injection added timezone offset display — format changed from `yyyy/mm/dd hh:mm:ss` to `yyyy/mm/dd hh:mm:ss (UTC±hh:mm)` |
| 4.2.4 | Removed overlay settle convergence debug logs (console.log in settle loop onDone callback) |
| [4.2.3](changelog/v4.md#423---2026-06-14) | Cleared all [DSS-DIAG] diagnostic logs; fixed preset-settle.scheduler.js onLog→onDone API mismatch (caused runSettle() to throw in guard clause, leaving dropdown blank); added API contract regression test |
| [4.2.2](changelog/v4.md#422---2026-06-14) | Fixed mobile overlay position race: added bounded settle retry loop (preset-settle.scheduler.js) measuring new-conversation button left per frame until stable for K consecutive frames; resolves button position shift during page load undetected by ResizeObserver |
| [4.2.1](changelog/v4.md#421---2026-06-14) | Fixed dropdown position calculation sub-pixel jitter and strengthened idempotency (Math.round + tail truncation dedup) |
| [4.2.0](changelog/v4.md#420---2026-06-14) | Refactored preset overlay: split large module into single-responsibility small files (controller, resolvers, position, styles, component) |
| [4.1.0](changelog/v4.md#410---2026-06-13) | Added mobile homepage cleanup module v4.1.0 |
| [4.0.0](changelog/v4.md#400---2026-06-13) | Large file modular refactoring: storage-manager, go-top, censor-reply-restore, content-script, popup split into single-responsibility modules; behavior unchanged; 985 tests all green |

### v3.x — Editor and Architecture Refinement

| Version | Summary |
|-|-|
| [3.4.0](changelog/v3.md#340---2026-06-13) | Auto-scroll after edit click, aligning edit box 16px below fixed header |
| [3.3.0](changelog/v3.md#330---2026-06-13) | Edit area max-height changed: `.cc852ac5` removed, `._646a522` dynamically computed; diagnostic logs removed |
| [3.2.3](changelog/v3.md#323---2026-06-13) | Fixed edit cleanup grabbing main input box; changed to detect newly appeared edit box after click |
| [3.2.2](changelog/v3.md#322---2026-06-13) | Edit cleanup diagnostic build (added `[DV:EditCleanup]` logs) |
| [3.2.1](changelog/v3.md#321---2026-06-13) | Auto-strip injection wrapper on edit message, keeping only `<user-input>` original text and expanding edit area |
| [3.2.0](changelog/v3.md#320---2026-06-09) | Added mobile sidebar right-swipe gesture (center 80% trigger zone) |
| [3.1.3](changelog/v3.md#313---2026-06-08) | Cleared all extension debug logs and dead code |
| [3.1.2](changelog/v3.md#312---2026-06-08) | GoToTop SPA route-change immediate retry injection fix |
| [3.1.1](changelog/v3.md#311---2026-06-08) | Internal diagnostic build — added route-change debug logs |
| [3.1.0](changelog/v3.md#310---2026-06-08) | GoToTop initial injection race condition fix; scroll now a clickable toggle |
| [3.0.0](changelog/v3.md#300---2026-06-07) | Standalone prompt editor window, global prompt toggle, popup refactoring |

### v2.x — Stability Hardening and Feature Expansion

| Version | Summary |
|-|-|
| [2.9.0](changelog/v2.md#290---2026-06-07) | CensorRestore expanded to intercept edit_message API |
| [2.8.12](changelog/v2.md#2812---2026-06-07) | Fixed CensorRestore same-conversation pending-queue misalignment |
| [2.8.11](changelog/v2.md#2811---2026-06-07) | Fixed CensorRestore SPA cross-conversation state contamination |
| [2.8.10](changelog/v2.md#2810---2026-06-07) | Fixed CensorRestore not triggering after page refresh |
| [2.8.9](changelog/v2.md#289---2026-06-07) | Fixed CensorRestore broken by ds-button redesign |
| [2.8.8](changelog/v2.md#288---2026-06-06) | Fixed GoToTop `_getNativeButton` selecting wrong button |
| [2.8.7](changelog/v2.md#287---2026-06-06) | GoToTop adapted to DeepSeek ds-button new design system |
| [2.8.6](changelog/v2.md#286---2026-06-06) | GoToTop border missing and mode-switch flicker fix |
| [2.8.5](changelog/v2.md#285---2026-06-06) | GoToTop added solo mode, fixed shape and overlap issues |
| [2.8.2](changelog/v2.md#282---2026-06-02) | Removed all Playwright integration tests |
| [2.7.3](changelog/v2.md#273---2026-06-02) | Fixed mobile send button injection |
| [2.7.2](changelog/v2.md#272---2026-06-01) | Fixed sidebar dropdown detection (`el.closest`) |
| [2.7.1](changelog/v2.md#271---2026-06-01) | Fixed hide thinking process collapse logic |
| [2.7.0](changelog/v2.md#270---2026-05-31) | Added system time injection feature |
| [2.6.2](changelog/v2.md#262---2026-05-29) | Export toast two-phase text; fixed scroll interruption detection |
| [2.6.1](changelog/v2.md#261---2026-05-29) | Prevent-auto-scroll patch; scroll_interrupted safety net |
| [2.6.0](changelog/v2.md#260---2026-05-29) | Scroll-and-harvest full Markdown export |
| [2.5.23](changelog/v2.md#2523---2026-05-29) | GoToTop button initial version |
| [2.5.15](changelog/v2.md#2515---2026-05-28) | Fixed resolveSyncConflict accidentally including restored_messages |
| [2.5.14](changelog/v2.md#2514---2026-05-28) | Fixed init migration push sync quota crash |
| [2.5.13](changelog/v2.md#2513---2026-05-28) | Fixed visibilitychange unhandled promise rejection |
| [2.5.12](changelog/v2.md#2512---2026-05-28) | Fixed getSettings fetching restored_messages causing crash |
| [2.5.11](changelog/v2.md#2511---2026-05-28) | Fixed page-refresh censor reply matching |
| [2.5.0](changelog/v2.md#250---2026-05-27) | Cross-context concurrency control (Method C lock + Method D CAS) |
| [2.4.1](changelog/v2.md#241---2026-05-27) | Fixed version not incrementing; bind insert read optimization |
| [2.4.0](changelog/v2.md#240---2026-05-27) | ChatPresetMap chunked storage (bypasses 8KB sync quota) |
| [2.3.0](changelog/v2.md#230---2026-05-26) | ChatPresetMap write queue (eliminates same-context races) |
| [2.1.1](changelog/v2.md#211---2026-05-24) | Fixed slider container spacing |
| [2.1.0](changelog/v2.md#210---2026-05-24) | Added hide thinking process feature |
| [2.0.0](changelog/v2.md#200---2026-05-23) | Sync write quota fix; cloud sync status indicator |

### v1.x — Initial Feature Development

| Version | Summary |
|-|-|
| [1.10.1](changelog/v1.md#1101---2026-05-23) | Fixed quote reply multi-line selection positioning |
| [1.10.0](changelog/v1.md#1100---2026-05-22) | Added quote reply feature |
| [1.9.0](changelog/v1.md#190---2026-05-20) | Custom combobox dropdown (search, inline buttons, pointer-event drag reorder) |
| [1.8.2](changelog/v1.md#182---2026-05-15) | Graceful handling of extension context invalidation |
| [1.8.1](changelog/v1.md#181---2026-05-15) | Fixed overlay conversation binding display |
| [1.8.0](changelog/v1.md#180---2026-05-14) | Added in-page overlay preset switcher |
| [1.7.2](changelog/v1.md#172---2026-05-14) | Fixed conversation switch losing UUID binding |
| [1.7.1](changelog/v1.md#171---2026-05-14) | Fixed stale prompt prefix; fixed sidebar dropdown collapse logic |
| [1.7.0](changelog/v1.md#170---2026-05-13) | Per-preset individual storage keys; Plan A local authoritative tracking |
| [1.6.6](changelog/v1.md#166---2026-05-12) | Fixed new tab cross-conversation prompt inheritance; fixed unintended auto-bind |
| [1.6.4](changelog/v1.md#164---2026-05-12) | Popup two-column layout |
| [1.6.3](changelog/v1.md#163---2026-05-12) | Allow deleting all custom presets |
| [1.6.2](changelog/v1.md#162---2026-05-12) | Fixed modal message line breaks |
| [1.6.1](changelog/v1.md#161---2026-05-11) | Sync conflict detection and resolution |
| [1.6.0](changelog/v1.md#160---2026-05-11) | chrome.storage.sync; JSON export and import |
| [1.5.5](changelog/v1.md#155---2026-05-11) | Fixed new chat stale selection |
| [1.5.4](changelog/v1.md#154---2026-05-10) | Fixed selection cleared on reopen |
| [1.5.3](changelog/v1.md#153---2026-05-10) | Fixed new chat first-send injection |
| [1.5.2](changelog/v1.md#152---2026-05-07) | Fixed sidebar dropdown hover detection |
| [1.5.1](changelog/v1.md#151---2026-05-07) | Popup multiple UI fixes; fixed cross-tab contamination |
| [1.5.0](changelog/v1.md#150---2026-05-07) | Sidebar auto-hide; conversation and input width adjusters |
| [1.4.6](changelog/v1.md#146---2026-05-06) | Export failure toast notification |
| [1.4.5](changelog/v1.md#145---2026-05-06) | Empty option always visible |
| [1.4.4](changelog/v1.md#144---2026-05-06) | Code block export fix |
| [1.4.3](changelog/v1.md#143---2026-05-06) | Delete falls back to empty state |
| [1.4.2](changelog/v1.md#142---2026-05-06) | Fixed new preset not reflected in dropdown on bound conversations |
| [1.4.1](changelog/v1.md#141---2026-05-06) | Fixed new conversation stale preset leak |
| [1.4.0](changelog/v1.md#140---2026-05-05) | Global default prompt; UUID conversation binding; SPA navigation detection |
| [1.3.0](changelog/v1.md#130---2026-05-05) | Multi-preset prompt management; custom modal system |
| [1.2.0](changelog/v1.md#120---2026-05-04) | Removed automatic injection separator |
| [1.1.1](changelog/v1.md#111---2026-05-03) | Heading (H1–H6) and table Markdown export support |
| [1.1.0](changelog/v1.md#110---2026-05-03) | Markdown export engine refactoring |
| [1.0.0](changelog/v1.md#100) | Initial release: Markdown export, base architecture |

### Detailed Changelogs

| Version Series | File |
|-|-|
| v4.x Details | [→ changelog/v4.md](changelog/v4.md) |
| v3.x Details | [→ changelog/v3.md](changelog/v3.md) |
| v2.x Details | [→ changelog/v2.md](changelog/v2.md) |
| v1.x Details | [→ changelog/v1.md](changelog/v1.md) |
